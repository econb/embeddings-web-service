services:
  - type: web
    name: embedding-service
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn enbedding-ws:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: OPENAI_API_KEY
        sync: false
